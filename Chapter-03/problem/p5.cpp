#include <iostream>

using namespace std;

int main(void) {
    long long world, us;
    cout << "Enter the world's population: " << ends;
    cin >> world;
    cout << "Enter the population of the US: " << ends;
    cin >> us;
    cout << "The population of the US is " << double(us) / double(world) * 100 << "% of the world population." << endl;
    return 0;
}